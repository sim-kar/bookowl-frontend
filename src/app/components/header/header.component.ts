import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  templateUrl: './header.component.html',
  styleUrls: ['./header.component.css'],
})

/** A header with the website's title, navigation and search bar. */
export class HeaderComponent {
}
